# Cutie 前端 i18n 迁移方案

## 📋 概述

本文档描述了将 Cutie 前端硬编码文本全面迁移到 i18n 国际化系统的完整方案。

**迁移范围**：12+ 个视图文件、20+ 个组件文件、300+ 个待国际化的文本字符串

---

## 🏗️ 术语体系分类

### 1. 导航与布局 (`nav`)
侧边栏导航菜单、区块标题等。

### 2. 工具栏视图 (`toolbar`)
右侧工具栏的视图切换按钮标签。

### 3. 任务相关 (`task`)
任务操作、占位符、标签、状态等。

### 4. 截止日期 (`dueDate`)
截止日期设置相关的文本。

### 5. 循环任务 (`recurrence`)
循环规则配置、状态、操作等。

### 6. 回收站 (`trash`)
回收站视图相关的所有文本。

### 7. 时间格式化 (`time`)
相对时间、日期范围等时间相关文本。

### 8. 日历 (`calendar`)
日历视图相关的操作和标签。

### 9. 设置 (`settings`)
设置面板的分类、字段、描述等。

### 10. Area 管理 (`area`)
Area 管理器相关的所有文本。

### 11. 通用操作 (`common`)
通用按钮、状态文本等。

### 12. 确认对话框 (`confirm`)
所有确认弹窗的消息文本。

### 13. 消息提示 (`message`)
成功/错误提示消息。

---

## 📝 迁移步骤

### Phase 1: 基础设施准备 ✅ 已完成

1. **更新语言文件结构**
   - `src/locales/zh-CN.ts` - 中文语言包（已更新）
   - `src/locales/en.ts` - 英文语言包（已更新）

2. **i18n 配置无需修改**（`src/i18n/index.ts` 已配置好）

---

### Phase 2: 组件迁移

#### 2.1 迁移模式

**模板中使用 `$t()`：**
```vue
<!-- 之前 -->
<span>暂存区</span>

<!-- 之后 -->
<span>{{ $t('toolbar.staging') }}</span>
```

**script 中使用 `useI18n()`：**
```typescript
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

// 使用
const label = t('toolbar.staging')
```

**带参数的翻译：**
```typescript
// 语言文件
{
  confirm: {
    restoreTask: '确定要恢复任务"{title}"吗？'
  }
}

// 使用
t('confirm.restoreTask', { title: task.title })
```

**复数形式（如需要）：**
```typescript
// 语言文件
{
  area: {
    count: '{n} 个 | {n} 个'  // 中文不区分单复数
  }
}

// 英文
{
  area: {
    count: '{n} area | {n} areas'
  }
}
```

---

#### 2.2 按优先级迁移文件列表

##### 🔴 高优先级（用户可见核心界面）

| 文件 | 预估文本数 | 说明 |
|------|------------|------|
| `MainLayout.vue` | 15+ | 侧边栏导航 |
| `TaskEditorModal.vue` | 25+ | 任务编辑器 |
| `KanbanTaskCardMenu.vue` | 12+ | 右键菜单 |
| `HomeView.vue` | 5 | 主视图 |
| `TrashView.vue` | 15+ | 回收站 |
| `QuickAddTaskModal.vue` | 5 | 快速添加 |

##### 🟡 中优先级（常用功能界面）

| 文件 | 预估文本数 | 说明 |
|------|------------|------|
| `RecurrenceManagerModal.vue` | 30+ | 循环任务管理 |
| `RecurrenceConfigDialog.vue` | 25+ | 循环配置 |
| `SettingsModal.vue` | 40+ | 设置面板 |
| `AreaManager.vue` | 15+ | Area 管理器 |
| `TimelineKanbanView.vue` | 20+ | 时间轴看板 |

##### 🟢 低优先级（次要界面）

| 文件 | 预估文本数 | 说明 |
|------|------------|------|
| `DailyOverviewView.vue` | 10+ | 每日概览 |
| `DailyShutdownView.vue` | 10+ | 每日收尾 |
| `CalendarKanbanView.vue` | 8 | 日历看板 |
| `StagingKanbanView.vue` | 5 | 暂存区看板 |
| `ProjectsView.vue` | 3 | 项目视图 |
| `UpcomingView.vue` | 3 | 即将到来 |

---

### Phase 3: 实施示例

#### 示例 1: MainLayout.vue 迁移

**迁移前：**
```vue
<li @click="...">
  <CuteIcon name="Clock" :size="16" /><span>Recent</span>
</li>
<li @click="...">
  <CuteIcon name="Layers" :size="16" /><span>Staging</span>
</li>
<div class="divider-label">DAILY ROUTINES</div>
```

**迁移后：**
```vue
<li @click="...">
  <CuteIcon name="Clock" :size="16" /><span>{{ $t('nav.recent') }}</span>
</li>
<li @click="...">
  <CuteIcon name="Layers" :size="16" /><span>{{ $t('nav.staging') }}</span>
</li>
<div class="divider-label">{{ $t('nav.section.dailyRoutines') }}</div>
```

---

#### 示例 2: 确认对话框迁移

**迁移前：**
```typescript
if (!confirm(`确定要恢复任务"${task.title}"吗？`)) return
```

**迁移后：**
```typescript
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

if (!confirm(t('confirm.restoreTask', { title: task.title }))) return
```

---

#### 示例 3: 时间格式化迁移

**迁移前：**
```typescript
function formatDeletedAt(deletedAt: string | null): string {
  if (!deletedAt) return '未知时间'
  // ...
  if (diffMins < 1) return '刚刚'
  if (diffMins < 60) return `${diffMins} 分钟前`
  if (diffHours < 24) return `${diffHours} 小时前`
  if (diffDays < 7) return `${diffDays} 天前`
  // ...
}
```

**迁移后：**
```typescript
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

function formatDeletedAt(deletedAt: string | null): string {
  if (!deletedAt) return t('time.unknown')
  // ...
  if (diffMins < 1) return t('time.justNow')
  if (diffMins < 60) return t('time.minutesAgo', { n: diffMins })
  if (diffHours < 24) return t('time.hoursAgo', { n: diffHours })
  if (diffDays < 7) return t('time.daysAgo', { n: diffDays })
  // ...
}
```

---

### Phase 4: 工具栏配置迁移

许多组件使用配置对象定义视图，如：

```typescript
// 迁移前
const viewConfig = {
  calendar: { icon: 'Calendar', label: '日历' },
  staging: { icon: 'Layers', label: 'Staging' },
}

// 迁移后 - 方案 A：使用计算属性
const { t } = useI18n()
const viewConfig = computed(() => ({
  calendar: { icon: 'Calendar', label: t('toolbar.calendar') },
  staging: { icon: 'Layers', label: t('toolbar.staging') },
}))

// 迁移后 - 方案 B：使用 key 引用
const viewConfig = {
  calendar: { icon: 'Calendar', labelKey: 'toolbar.calendar' },
  staging: { icon: 'Layers', labelKey: 'toolbar.staging' },
}
// 在模板中: {{ $t(config.labelKey) }}
```

---

## 🔧 技术注意事项

### 1. 导入 useI18n

在每个需要 i18n 的组件中：

```typescript
import { useI18n } from 'vue-i18n'

// 在 setup 中
const { t } = useI18n()
```

### 2. 响应式语言切换

如果需要支持运行时语言切换：

```typescript
import { useI18n } from 'vue-i18n'

const { locale } = useI18n()

// 切换语言
locale.value = 'en'
```

### 3. 日期本地化

对于日期显示，建议使用 `toLocaleDateString`：

```typescript
date.toLocaleDateString(locale.value, {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
})
```

### 4. 避免硬编码 confirm()

原生 `confirm()` 无法国际化按钮文字（确定/取消），如需完全国际化，考虑使用自定义确认对话框组件。

---

## 📊 迁移进度追踪

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 1: 语言文件 | ✅ 完成 | zh-CN.ts, en.ts 已更新 |
| Phase 2: 高优先级组件 | ⏳ 待开始 | 6 个文件 |
| Phase 3: 中优先级组件 | ⏳ 待开始 | 5 个文件 |
| Phase 4: 低优先级组件 | ⏳ 待开始 | 6 个文件 |
| Phase 5: 测试与验证 | ⏳ 待开始 | 全面测试 |

---

## 📎 附录：语言文件结构一览

```
src/locales/
├── zh-CN.ts          # 中文（简体）
└── en.ts             # 英文
```

**顶层 Key 结构：**
- `nav` - 导航
- `sidebar` - 侧边栏（兼容旧结构）
- `toolbar` - 工具栏
- `task` - 任务
- `dueDate` - 截止日期
- `recurrence` - 循环任务
- `trash` - 回收站
- `time` - 时间
- `calendar` - 日历
- `settings` - 设置
- `area` - Area
- `quickAdd` - 快速添加
- `view` - 视图标题
- `common` - 通用
- `confirm` - 确认对话框
- `message` - 消息提示
- `ai` - AI 助手
- `projects` - 项目（占位）
- `experience` - 经历（占位）

---

**文档版本**：v1.0  
**最后更新**：2025-11-27  
**作者**：AI Assistant

